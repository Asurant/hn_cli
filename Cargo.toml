[package]
name = "hn_cli"
version = "0.1.0"
edition = "2024"
description = "A simple CLI that prints out the top 25 hacker news posts"
license = "MIT"

[dependencies]
reqwest = {version = "0.12", features = ["json", "blocking"]}
serde = {version = "1", features = ["derive"]}
